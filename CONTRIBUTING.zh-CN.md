# 贡献指南

[English](CONTRIBUTING.md) · [路线图](docs/roadmap.zh-CN.md) · [本地管理](docs/development.zh-CN.md)

欢迎使用中文或英文参与。改进主机测试、文档、构建脚本和设备模型源码不需要固件镜像。

## 修改之前

先阅读[架构](docs/architecture.zh-CN.md)与[状态](docs/status.zh-CN.md)，选择范围明确的问题。较大的图形或板级修改，建议先在 Issue 中说明目标行为，便于集中实施和审查。

## 开发与验证

```sh
python3 -B -m unittest discover -s scripts/harmattan-qemu/tests -p 'test_*.py'
python3 scripts/check-public-tree.py
git diff --check
```

修改应落实到维护中的补丁文件；解包后的 QEMU 树属于构建产物。使用新的 `HARMATTAN_PORT_WORKSPACE` 验证补丁序列，不能以增量构建代替从干净源码应用补丁的验证。

涉及客体行为的改动，应附准确启动命令、宿主和客体版本、输入标识、正例及相关负例结果，以及简短诊断结论。分别说明覆盖了主机逻辑、无窗口客体、Cocoa 窗口还是实际鼠标交互。客体 RAM 帧采样不等于屏幕 FPS。

不要仅为让结果通过而放宽验证器。修改预期时说明依据，并保留未知 GPU 故障、生命周期错误和客体命令失败的检查。尽可能保留原版 UI 行为，通过明确的兼容辅助代码适配，不要静默替换产品图形或编造系统数据。

## Pull Request

- 说明具体问题及修改后的行为。
- 保持范围可复现、可审查。
- 文档变更同步两种语言；需要翻译帮助时在 PR 中说明。
- 保留继承的版权及许可声明，标明引入代码的来源。
- 附验证结果及尚未覆盖的范围。
- 提交源码、文档及审阅过的最小诊断片段。运行截图需要采集来源和画面审阅，发布检查只允许明确列入清单的截图。不要上传固件、SDK 包、个人客体数据库、凭据、完整内存转储或私人路径。

提交代码意味着你确认有权按对应文件的许可贡献这些内容。本项目不要求额外的贡献者协议。

CI 运行主机测试和公开文件检查，不获取专有输入，也不代表完整客体兼容性。Linux 跳过 AppKit 测试，macOS 执行这些原生测试。
